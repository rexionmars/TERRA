"""
The ground itself, arranged for a viewer.

One action and no module of its own, for the same reason terra/scenes has
none: what it answers is the terrain reader's output shaped for the caller
rather than a computation over it. It sits here rather than in terra/terrain
so that package stays a reader.
"""
